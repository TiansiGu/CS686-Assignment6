micronaut:
  application:
    name: micronaut-crud
datasources:
  default:
    url: jdbc:mysql://127.0.0.1:3306/micronaut_db
    username: root
    password: password

flyway:
  datasources:
    default:
      enabled: true
